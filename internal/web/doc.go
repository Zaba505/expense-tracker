// Package web wires the net/http ServeMux, handlers, and middleware
// (including the owner-allowlist auth middleware) that serve the HTMX
// application.
//
// Implemented by later stories (see issues for `story(web)`, `story(auth)`,
// and `story(entry)`).
package web
