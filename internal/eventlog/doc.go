// Package eventlog provides the append-only event store: an EventStore
// interface with Firestore and in-memory implementations. Events are
// written immutably and loaded in deterministic order for folding.
//
// Implemented by a later story (see issue for `story(eventlog)`).
package eventlog
